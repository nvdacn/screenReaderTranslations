
-{-- userGuide.t2t}-

+{++ /tmp/revs/9286/userGuide.t2t}+
	2013-07-23 
-{08:28:48.332276089}-
 
+{08:29:14.492718545}+
 +0200
@@ -302,7 +302,7 @@

Object navigation allows you to move between and obtain information about individual [objects #Objects].
When you move to an object, NVDA will report it similarly to the way it reports the system focus.
For a way to review all text as it appears on the screen, you can instead use 
-{[flat}-
 
+{[screen}+
 review 
-{#FlatReview].}-
 
+{#ScreenReview].}+


Rather than having to move back and forth between every single object on the system, the objects are organized hierarchically.
This means that some objects contain other objects and you must move inside them to access the objects they contain.
@@ -313,7 +313,7 @@
Similarly, a toolbar contains controls, so you must move inside the toolbar to access the controls in the toolbar.

The object currently being reviewed is called the navigator object.
Once you navigate to an object, you can review 
-{it}-
 
+{its content}+
 using the [text review commands 
-{#ReviewingText].}-
 
+{#ReviewingText] while in [Object review mode #ObjectReview].}+

By default, the navigator object moves along with the System focus, though this behaviour can be toggled on and off.

Note that braille follows the [focus #SystemFocus] and [caret #SystemCaret] by default, rather than object navigation and text review.
@@ -337,7 +337,7 @@
Note: numpad keys require numlock key to be turned off to work properly.

++ Reviewing Text ++[ReviewingText]
NVDA allows you to read the contents of the 
+{[screen #ScreenReview],}+
 current 
-{[navigator object #ObjectNavigation] (including [flat review #FlatReview])}-
 
+{[document #DocumentReview] or current [object #ObjectReview]}+
 by character, word or line.
This is mostly useful in places (including Windows command consoles) where there is no [system caret #SystemCaret].
For example, you might use it to review the text of a long information message in a dialog.

@@ -378,28 +378,36 @@
| Previous word | Current word | Next word |
| Previous character | Current character | Next character |

++ 
-{Flat}-
 Review 
-{++[FlatReview]
While you}-
 
+{Modes ++[ReviewModes]
NVDA's review commands}+
 can 
-{generally only}-
 review 
-{the}-
 content 
-{of a single [object #Objects], some objects allow you to}-
 
+{within the current navigator object, current document, or screen, depending on the}+
 review 
+{mode selected.
Review modes are a replacement for}+
 the 
-{content of objects inside them.
Complex documents allow you to}-
 
+{older Flat Review concept found in NVDA.

The following commands switch between}+
 review 
-{their entire content and application windows allow you}-
 
+{modes:
%kc:beginInclude
|| Name | Desktop key | Laptop key | Touch | Description |
| switch}+
 to 
+{next}+
 review 
+{mode | NVDA+numpad7 | NVDA+pageUp | 2-finger flick up | switches to}+
 the 
-{text of}-
 
+{next available review mode higher than}+
 the 
-{screen as it appears visually.
This is called flat review.
This is similar}-
 
+{current mode. | 
| switch to previous review mode | NVDA+numpad1 | NVDA+pageDown | 2-finger flick down | switches}+
 to the 
-{screen}-
 
+{next available}+
 review 
-{or mouse cursor functionality}-
 
+{mode deeper than the current mode. |
%kc:endInclude

+++ Object Review +++[ObjectReview]
While}+
 in 
-{many other Windows screen readers.

When}-
 
+{object review mode,}+
 you 
-{move}-
 
+{are able}+
 to 
-{flat review, you will be positioned at}-
 
+{only review}+
 the 
-{location}-
 
+{content}+
 of the current [navigator object #ObjectNavigation].

-{If you haven't used object navigation,}-


+{For objects such as edit fields or other basic document controls,}+
 this will 
-{usually}-
 
+{generally}+
 be the 
-{object that has the [focus #SystemFocus].}-
 
+{text content.}+

For 
-{example, if}-
 
+{other objects, this may be its name and or value.

+++ Document Review +++[DocumentReview]
When}+
 the 
-{navigator}-
 
+{[navigator}+
 object 
-{was}-
 
+{#ObjectNavigation] is within}+
 a 
-{button,}-
 
+{browse mode document (e.g. web page) or another complex document containing many objects (e.g. Lotus Symphony documents), it is possible to switch to the document review mode.
The document review mode allows}+
 you 
-{will usually be positioned on}-
 
+{to review}+
 the text of the 
-{button in flat review.
You can then}-
 
+{entire document.
When switching from object}+
 review 
+{to document review,}+
 the 
-{text using the [text}-
 review 
-{commands #ReviewingText].
You can also navigate directly to}-
 
+{cursor is placed in}+
 the 
-{object encompassing}-
 
+{document at}+
 the 
-{current}-
 position of the 
+{navigator object.
When moving around the document with}+
 review 
-{cursor, allowing you}-
 
+{commands, the navigator object is automatically updated}+
 to 
-{use [object navigation #ObjectNavigation] from there.

Note that braille follows}-
 the 
-{[focus #SystemFocus] and [caret #SystemCaret] by default, rather than}-
 object 
-{navigation and text review.
If you want it}-
 
+{found at the current review cursor position.
Note that NVDA will switch}+
 to 
-{follow}-
 
+{document review from}+
 object 
-{navigation and text}-
 review 
-{instead,}-
 
+{automatically when moving around browse mode documents.

+++ Screen Review +++[ScreenReview]
The screen review mode allows}+
 you 
-{need to [configure braille}-
 to 
-{be tethered}-
 
+{review visible text on the screen within the current application.
This is similar}+
 to 
-{#BrailleTether] review.

The following two commands are used for}-
 
+{the screen review or mouse cursor functionality in many other Windows screen readers.
 When}+
 switching 
-{to/from flat review:
%kc:beginInclude
|| Name | Desktop key | Laptop key | Touch | Description |
| Move}-
 to 
-{flat}-
 
+{screen}+
 review 
-{| NVDA+numpad7 | NVDA+pageUp | 2-finger flick up | Moves to flat review, placing you}-
 
+{mode, the review cursor is placed}+
 at the 
+{screen}+
 position of the current 
-{navigator object, allowing you to review}-
 
+{[navigator object #ObjectNavigation].
 When moving around}+
 the screen 
-{(or document if you are currently inside one)}-
 with 
-{the text}-
 review 
-{commands. |
| Move to}-
 
+{commands, the navigator}+
 object 
-{from flat review | NVDA+numpad1 | NVDA+pageDown | 2-finger flick down | navigates}-
 
+{is automatically updated}+
 to the 
+{deepest}+
 object 
-{represented by the text}-
 
+{found}+
 at the 
-{current}-
 
+{screen}+
 position of the review 
-{cursor}-
 
+{cursor.
Note that}+
 in 
-{flat review |
%kc:endInclude}-
 
+{some newer applications, NVDA may not see some or all text available on screen, due to the use of newer screen drawing technologies which are impossible to support at this time.}+

  
++ Navigating with the Mouse ++
When you move the Mouse, NVDA by default reports the text that is directly under the mouse pointer as the pointer moves over it.
Where supported, NVDA will read the surrounding paragraph of text, though some controls may only read by line.
@@ -951,12 +959,9 @@
This checkbox toggles the automatic reading of a page after it loads in browse mode.
This option is enabled by default.

==== 
-{Include}-
 
+{Report}+
 layout tables ====

-{This}-


+{When disabled this}+
 option 
-{affects how}-
 
+{makes}+
 NVDA 
-{handles}-
 
+{only report}+
 tables 
-{used purely for layout purposes.
When on,}-
 
+{that contain tabular data (where it makes sense to know that this is a table).
But if enabled,}+
 NVDA will 
-{treat these as normal tables, reporting them based on [Document Formatting Settings #DocumentFormattingSettings] and locating them with quick navigation commands.
When off, they will not be reported nor found with quick navigation.
However, the content of the}-
 
+{also report}+
 tables 
-{will still be included as normal text.
This option is turned off by default.}-
 
+{used purely for visual presentation.}+


==== Configuring reporting of fields such as links and headings ====
Please see the options in the [Document Formatting Settings dialog #DocumentFormattingSettings] to configure the fields that are reported when navigating, such as links, headings and tables.
@@ -1094,7 +1099,7 @@
The Add-ons Manager contains a list that displays all the add-ons currently installed in your NVDA user configuration. 
A package name, version and author are shown for each add-on, though further information such as a description and URL can be viewed by selecting the add-on and pressing the About add-on button.

To browse and download 
-{available}-
 
+{availible}+
 add-ons online, press the Get add-ons button.
This button opens the [NVDA Add-ons page http://addons.nvda-project.org/].
If NVDA is installed on your system, you can open the add-on directly from the browser to begin the installation process as described below.
Otherwise, save the add-on package and follow the instructions below.
